#include <stdio.h>

int main(void) {
    int line = 0;

    printf("Add in bit\n");
    while (line < 10){
        printf("Code one line ");
        line++;
        printf("%d\n", line);
    }
    return 0;
}